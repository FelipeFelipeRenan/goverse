apiVersion: 1
providers:
  - name: 'goverse-dashboards'
    folder: 'Goverse'
    type: file
    updateIntervalSeconds: 5
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
